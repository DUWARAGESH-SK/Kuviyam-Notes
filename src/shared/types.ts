/**
 * Shared TypeScript types for Kuviyam Notes.
 * Single source of truth — import from here, not from individual modules.
 */
export type { Note, NoteDraft, PanelLayout, Folder, AppSettings } from '../types';
